"""The engine's "no function" id (`Function::invalidId()`).

A blackout button, an unbound slider or a track without a scene writes it. It
refers to nothing on purpose.
"""

INVALID_ID = "4294967295"
