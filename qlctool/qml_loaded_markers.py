"""The lines the QLC+ 5 QML build logs once a workspace is on screen.

It never goes quiet on its own, so these are what "loading finished" means
there.
"""

QML_LOADED_MARKERS = ("renderPage", "MasterTimer", "Time is late")
